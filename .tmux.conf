# Prefix binding {{{
set -g prefix C-g
unbind C-b
bind g  send-prefix
bind-key C-g last-window
# }}}

# General {{{
set utf8 on
set -sg escape-time 0
setw -g aggressive-resize on
bind R source-file ~/.tmux.conf
# }}}

# Status bar {{{
set -g status-bg black
set -g status-fg colour239
set -g status-left-length 20
set -g status-left '#[fg=magenta]#S #[fg=cyan]»'
set -g status-right '#(cat ~/.unread) #(cat ~/.unread_dev) #(cat ~/.unread_reader) #[fg=cyan]« #[fg=yellow]#h'
set-window-option window-status-fg colour239
set-window-option window-status-bg black

# Highlight current window {{{
set-window-option -g window-status-current-fg white
set-window-option -g window-status-current-bg black
# }}}

# Activity window {{{
set-window-option -g window-status-activity-fg cyan
set-window-option -g window-status-activity-bg black
# }}}
# Content window {{{
set-window-option -g window-status-content-fg magenta
set-window-option -g window-status-content-bg magenta
# }}}

# Highlight active window {{{
set-window-option -g window-status-bell-fg magenta
set-window-option -g window-status-bell-bg magenta
# }}}

# window format {{{
setw -g window-status-format '#[fg=colour235,bg=black,noreverse]#I/#[default,noreverse]#W'
setw -g window-status-current-format '#[fg=magenta]#I#[fg=colour235]/#[default,noreverse]#W'
# }}}
# }}}

# Set window notifications {{{
setw -g monitor-activity on
set -g visual-activity on

# Automatically set window title
setw -g automatic-rename
# }}}

# window management {{{
unbind %
unbind s
bind | split-window -h
bind _ split-window -v
bind-key S command-prompt "attach-session -t '%%'"
# }}}

# Vi style bindings {{{
setw -g mode-keys vi
unbind Escape
unbind -t vi-copy Space
unbind -t vi-copy Enter
bind -t vi-copy v begin-selection
bind -t vi-copy y copy-selection

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
# }}}

# Resizing {{{
bind < resize-pane -L 5
bind > resize-pane -R 5
bind - resize-pane -D 5
bind + resize-pane -U 5
# }}}
